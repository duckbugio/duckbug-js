# Руководство по релизам

Этот проект использует автоматическое управление версиями и релизами через [semantic-release](https://github.com/semantic-release/semantic-release).

## Как работает автоматическое версионирование

### Типы коммитов и версии

| Тип коммита | Тип релиза | Пример изменения версии |
|------------|-----------|------------------------|
| `feat:` | Minor | 1.0.0 → 1.1.0 |
| `fix:` | Patch | 1.0.0 → 1.0.1 |
| `perf:` | Patch | 1.0.0 → 1.0.1 |
| `BREAKING CHANGE` или `feat!:` | Major | 1.0.0 → 2.0.0 |
| `docs:`, `style:`, `refactor:`, `test:`, `build:`, `ci:`, `chore:` | - | Без релиза |

### Что происходит при пуше в `main`

1. ✅ Запускаются тесты и линтер
2. 📊 Анализируются коммиты с последнего релиза
3. 🔢 Определяется новая версия (если есть изменения, требующие релиза)
4. 📝 Генерируется CHANGELOG.md
5. 📦 Обновляется версия в package.json
6. 🏷️ Создается git тег
7. 📤 Публикуется в npm
8. 🎉 Создается GitHub Release с заметками

## Примеры коммитов

### Патч релиз (1.0.0 → 1.0.1)
```bash
fix: исправить обработку ошибок в DuckBugProvider
```

### Минорный релиз (1.0.0 → 1.1.0)
```bash
feat: добавить поддержку фильтрации логов
```

### Мажорный релиз (1.0.0 → 2.0.0)

Вариант 1: Использование `!` в типе
```bash
feat!: изменить API провайдеров
```

Вариант 2: Использование `BREAKING CHANGE` в теле коммита
```bash
feat: добавить новую функцию

BREAKING CHANGE: изменена структура конфигурации DuckBugProvider
```

### Без релиза (версия не меняется)
```bash
docs: обновить README
chore: обновить зависимости
test: добавить тесты
style: исправить форматирование
```

## Настройка

### Для публикации в npm

1. Создайте npm токен:
   - Перейдите на https://www.npmjs.com/settings/YOUR_USERNAME/tokens
   - Создайте токен типа `Automation`
   - Скопируйте токен

2. Добавьте токен в GitHub Secrets:
   - Откройте настройки репозитория → Secrets and variables → Actions
   - Создайте новый секрет с именем `NPM_TOKEN`
   - Вставьте токен npm

### Для бета-релизов

Бета-релизы создаются автоматически при пуше в ветку `beta`:
- Версия будет иметь суффикс `-beta.X`
- Например: `1.1.0-beta.1`, `1.1.0-beta.2`
- Бета-релизы также публикуются в npm с тегом `beta`

## Ручной запуск (для тестирования)

Для локального тестирования можно запустить semantic-release в dry-run режиме:

```bash
bunx semantic-release --dry-run
```

**Примечание**: Для реального релиза используйте GitHub Actions workflow, который запускается автоматически.

## Откат релиза

Если нужно откатить релиз:

```bash
# Создайте коммит с типом revert
git revert <commit-hash>
git commit -m "revert: откат версии X.X.X"
git push
```

Semantic-release автоматически создаст патч-релиз с откатом изменений.
